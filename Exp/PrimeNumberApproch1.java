public class PrimeNumberApproch1 {
    /*
     * Before to code for prime number
     * first we need to understand what is prime number?
     * 
     * prime number:
     * 'n' is called as prime if it has only two perfect divisors that is 1 'n'
     * itself
     * 
     * Algorithm:-------------------------------------------------------------------
     * -----
     * step 1:
     * input positive integer 'num'
     * step 2:
     * check that is there any perfect divisor exists between range 2 to (num-1)
     * srep 3:
     * if divisor exists :print not prime number
     * else :print prime number
     * -----------------------------------------------------------------------------
     * ------
     * 
     */
    public static void main(String[] args) {
        // step1:declaring variable which stores integer
        // int num = 101, i;

        // // step 2:checking there any perfect divisor exists between range [2 to (num-1)]
        // for (i = 2; i < num; i++) {
        //     if (num % i == 0) {
        //         break;
        //     }
        // }

        // // step 3: desicion making
        // if (i == num) {
        //     System.out.printf("\n %d is a prime number..!",num);
        // } else {
        //     System.out.printf("\n %d is NOT a prime number..!",num);
        // }
    }
}
